import { createFileRoute } from "@tanstack/react-router";
import { Link } from "@tanstack/react-router";
import { ArrowRight } from "lucide-react";
import { PageIntro } from "@/components/site-shell";
import { categories } from "@/lib/catalog";
import { useLanguage } from "@/lib/i18n";
export const Route = createFileRoute("/products")({head:()=>({meta:[{title:"Towary | Auto Parts Store in Maldyty"},{name:"description",content:"Kategorie części samochodowych, olejów, filtrów i akcesoriów dostępnych w Małdytach."},{property:"og:title",content:"Towary | Auto Parts Store in Maldyty"},{property:"og:description",content:"Sprawdź zakres produktów naszego sklepu."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Products});
function Products(){const{language}=useLanguage();const pl=language==="pl";return <><PageIntro eyebrow={pl?"Towary":"Products"} title={pl?"Wszystko, czego potrzebuje Twoje auto":"Everything your car needs"} description={pl?"Poniżej znajdziesz główne grupy produktów. Zadzwoń lub wyślij zapytanie, aby potwierdzić dostępność konkretnej części.":"Explore our main product groups. Call or send an enquiry to confirm a specific part’s availability."}/><section className="mx-auto max-w-7xl px-4 py-6 sm:px-6"><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{categories.map((item,index)=><article className="glass-shell flex min-h-64 flex-col p-6" key={item.pl}><div className="flex items-center justify-between"><span className="grid size-12 place-items-center rounded-xl bg-primary/10 text-primary"><item.icon className="size-6"/></span><span className="text-xs font-bold text-muted-foreground">0{index+1}</span></div><h2 className="mt-8 font-display text-xl font-bold">{pl?item.pl:item.en}</h2><p className="mt-3 text-sm leading-relaxed text-muted-foreground">{pl?item.plText:item.enText}</p><Link to="/" hash="enquiry" className="mt-auto flex items-center gap-2 pt-6 text-sm font-bold text-primary">{pl?"Zapytaj o część":"Ask about a part"}<ArrowRight className="size-4"/></Link></article>)}</div></section></>}
